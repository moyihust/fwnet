//
// Created by 14413 on 2023/1/25.
//
#include "stdio.h"
#include "math.h"
#include "string.h"
/*
typedef struct point
{
    double x,y;
}point;
typedef struct boundary
{
    double x;   //中心横坐标
    double y;   //中心纵坐标
    double w;   //宽度
    double h;   //长度
}boundary;
typedef struct Quadtree
{
    double power;
    point pos;
    boundary boundary1;
    int captical;       //承载能力
    int num;
    char type[5];
    struct Quadtree *southWest;     //西南的子节点
    struct Quadtree *southEast;     //东南的子节点
    struct Quadtree *northWest;     //西北的子节点
    struct Quadtree *northEast;     //东北的子节点
}Quadtree,*Qt;
Qt CreateTree(point pos, int num, char type[5],boundary) // 创建节点
{
    auto t = (Qt)malloc(sizeof(Quadtree));
    strcpy(t->type, type);

    return t;
}
*/
